# FastAPI backend dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.37.0
python-multipart>=0.0.20

# Strands Agents and MCP dependencies
strands-agents>=1.12.0
python-mcp>=1.0.1
pyyaml>=6.0.0

# Note: For best compatibility, deploy to Linux EC2 instance
# Windows may require additional setup for MCP servers
